# Sysadminscripts

```
.
├── bind_reload.sh    # Relee el fichero de zona e incrementa a uno el serial según la fecha.
└── sshcheck.sh       # Comprueba si algún equipo de la red es accesible vía SSH con una lista de usuarios.
```
